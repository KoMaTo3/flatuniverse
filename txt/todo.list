- !!! в файле сохранени€ хранить версию не мира в целом, а версию каждого грида

- возможность указывать, какие объекты не должны сохран€тьс€ (ObjectAttr( objectName, { save = false } ))

- разделить "исходник мира" и "сохранЄнные изменени€"

- редактор: буфер действий
- редактор: выделение нескольких объектов
- редактор: элемент отображени€ DOM-модели

- сохранение прив€зок коллизий к луа

- сохранение/загрузка триггеров:
  - прив€занных функций
  - списка объектов в триггере

- анимаци€:
  - некий объект + ObjectPointer, периодически измен€ющий float/Vec2/Vec3/Vec4
  - возможность указывать вид интерпол€ции: Linear/Bezie/Catmull
  - возможность смены текстуры или не трогать текстуру вообще

- lua, обработка событий объекта: возможно, сделать дл€ каждого объекта сцены некий table-объект. Ќапример, объект "гриб":
  DOM.mushroom_abcdef0912346678 = { -- где abcdef0912346678 - md5( getObjectFullName )
    var0 = 0, -- произвольный список переменных объекта
    var1 = 1,
    ...
    varX = x,
    function0 = function() -- произвольный списокфункций объекта
    end,
    ...
    functionX = functionX()
    end,
  }

- освещение:
  - вли€ют только коллизии
